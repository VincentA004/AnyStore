# Shared Backend Contracts

Shared contracts live here so the managed proxy, frontend, and agentic backend can agree on stable request and response shapes before the backend is fully implemented.

Keep this folder lightweight. Runtime-specific code belongs in `backend/functions` or `backend/agentic`.
